{
  "image": "tensorflow/tensorflow:2.12.0-gpu-jupyter",
  "mounts": [{ "source": "e:/workspace", "target": "/workspace", "type": "bind" }, 
             { "source": "\\\\wsl.localhost\\Ubuntu\\home\\squll1\\.ssh", "target": "/root/.ssh", "type": "bind" }],
  "runArgs": [ "--gpus", "all", "--ipc=host", "--ulimit", "memlock=-1", "--ulimit", "stack=67108864" ],
  "postCreateCommand": "bash init_docker.sh",
  "forwardPorts": [6006],
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Add the IDs of extensions you want installed when the container is created.
      "extensions": ["ms-python.python"]
    }
  }
}